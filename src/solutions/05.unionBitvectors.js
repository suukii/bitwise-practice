/**
 * Union bit vector a with b.
 * @example
 * // returns 0b1111
 * unionBitvectors(0b1010, 0b0101)
 * @param {number} a
 * @param {number} b
 */
export const unionBitvectors = (a, b) => {
  return a | b;
};
