/**
 * Toggle the `n`th bit of |x|.
 * @example
 * // returns 0b0000
 * toggleBit(0b0010, 1)
 *  * @example
 * // returns 0b0010
 * toggleBit(0b0000, 1)
 * @param {number} x
 * @param {number} n
 */
export const toggleBit = (x, n) => {
  // code here
};
